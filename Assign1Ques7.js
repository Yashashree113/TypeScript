"use strict";
var check = names.find(function (element1) { return element1 === names[2]; });
var organization = {
    name: "Astha",
    address: {
        area: "Mumbai",
        pincode: 410206
    }
};
